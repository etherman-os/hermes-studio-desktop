# py-adapter

Future home for the Hermes Desktop Studio Python adapter.

Currently, the working adapter lives in `packages/hermes_adapter/`. This directory is reserved for a future rename/cleanup. Do not move code here yet — stabilize the desktop shell and protocol first.
